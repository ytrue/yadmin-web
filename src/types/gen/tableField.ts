export interface ITableFieldDataForm {
    sort: number | null,
    columnName: string | null,
    attrName: string | null,
    attrType: string | null,
    columnComment: string | null,
    isList: number | null,
    isQuery: number | null,
    queryType: string | null,
    isForm: number | null,
    isRequired: number | null,
    formType: string | null,
    tableId: number
}

